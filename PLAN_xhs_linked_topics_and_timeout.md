# 小红书可点击话题与定时任务超时修复

## Context
- 2026-07-14 定时任务未发布：Serenity 对 `supercycle.fi` 两个端点持续 TLS 握手超时，两次 300 秒外层尝试耗尽后没有本轮元数据。
- Hermes 的 900 秒前置脚本超时后仍启动 Agent；Agent 又重跑整套流程，等待后台进程期间会话结束，导致 job 状态未写回。
- 当前 `publish_xhs_ai.py` 把 `#标签` 拼进正文并一次性 `fill`，没有点击小红书话题联想候选，因此只产生普通文本。
- 参考 `xiaohongshu-mcp/xiaohongshu/publish.go::inputTag`：逐个输入 `#` + 标签并点击 `#creator-editor-topic-container .item`。

## Step 1: 修复话题输入 ✅
**File**: `scripts/publish_xhs_ai.py` — 修改

- 正文只填写文章内容，不拼接标签文本。
- 先让 `xhs_ai_publisher` 准备编辑表单，再逐个输入话题并点击联想候选。
- 任一话题未形成候选时中止发布，不能静默退化为普通文本。
- 复用现有标题、图片、登录态与发布后标题核验逻辑。

## Step 2: 增加话题回归测试 ✅
**File**: `tests/test_publish_xhs_ai.py` — 新增

- 验证正文与话题分离。
- 验证每个话题必须点击候选；候选缺失会失败。
- 验证最后两个标签仍由 payload 保证为 `白毛女神`、`长期主义`。

## Step 3: 核验定时任务预算与状态行为 ⚠️
**File**: `../investment-monitor-suite/bin/run_daily_suite.py` — 仅核验，本次不修改

- 原始 21:15 run 已正确记录为 `error`；飞书主日报及失败通知均成功送达。
- Hermes Agent 随后补跑，端点恢复后于 21:57 成功发布今日文章，发布历史只写入确认成功结果。
- 今日端点故障持续约 37 分钟，无法靠 900 秒单次超时覆盖；需要独立的延迟补偿运行，而不是继续放大单次子步骤超时。
- 本次不缩短现有重试，避免把暂时网络故障更快转化为漏发；也不把旧稿冒充本轮稿件。

## 实施顺序
1. ✅ 实现可点击话题输入与发布。
2. ✅ 增加并运行回归测试。
3. ⚠️ 核验超时与补跑状态；独立补偿调度尚未配置。

## 关键文件清单
| 文件 | 操作 | 状态 |
|---|---|---|
| `scripts/publish_xhs_ai.py` | 修改 | ✅ |
| `tests/test_publish_xhs_ai.py` | 新增 | ✅ |
| `../investment-monitor-suite/bin/run_daily_suite.py` | 核验 | 无需修改 |

## 验证状态
| 验证项 | 状态 |
|---|---|
| 话题候选选择单元测试 | ✅ 3/3 |
| Serenity 测试集 | ✅ 41/41 |
| Suite 测试集 | ✅ 20/20 |
| Python 语法编译 | ✅（缓存重定向到 `/tmp`） |
| 真实发布验收 | ✅ 2026-07-14 重发成功；两图、10 个链接话题 |
| 真实发布 | 不执行，避免重复/外部写入 |

## 遗留项 (Blockers)
| 项目 | 说明 | 优先级 |
|---|---|---|
| 长时间上游不可用 | 900 秒内无法覆盖今日约 37 分钟的端点故障；需要后续补偿运行策略 | 高 |
| 锁屏运行条件 | 锁屏可行，但系统睡眠、注销、FileVault 登录前不可行 | 中 |
